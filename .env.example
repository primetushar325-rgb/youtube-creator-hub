# YouTube Creator Hub — Environment Variables (server-side only, NEVER commit real keys)
# Copy to .env for local dev, OR set in Vercel Project Settings -> Environment Variables.

# Primary AI provider (text generation + Whisper transcription)
GROQ_API_KEY=

# Fallback provider (used when Groq returns 429 rate-limit)
GEMINI_API_KEY=
